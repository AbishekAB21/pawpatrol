import 'package:flutter/material.dart';

class WishlistScreenComponent extends StatelessWidget {
  const WishlistScreenComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Wishlist"),
    );
  }
}